version: '3.8'

services:
  intergram:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: intergram
    restart: always
    ports:
      - "${HOST_PORT:-0}:3000"
    environment:
      - PORT=3000
      - TELEGRAM_TOKEN=${TELEGRAM_TOKEN:-your_bot_token_here}
